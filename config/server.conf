# Basic srVR configuration file.
#
# The syntax is deliberately simple: one key=value pair per line.
# Blank lines and lines starting with '#' are ignored.

port=8080
root=./www
# Uncomment to enable the epoll-based event loop:
# use_epoll=1

